rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /dd_cms/main_data {
      allow read, write: if true;
    }
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
