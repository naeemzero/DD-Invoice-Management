{
  "name": "dd-invoice-management",
  "version": "1.0.0",
  "description": "Contribution & Invoice Management System for Dream Development",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p public && cp -r css js json index.html public/ 2>/dev/null || true",
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 0",
    "lint": "echo \"No linting required\" && exit 0"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
